version: 0.1
frontend:
  phases:
    preBuild:
      commands:

    # IMPORTANT - Please verify your build commands
    build:
      commands:
        - npm install
          - make build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /_site
    files:
      - "**/*"
  cache:
    paths: []
